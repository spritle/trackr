//command for generate the key store
keytool -genkey -v -keystore trackr.keystore -alias trackr  -keyalg RSA -keysize 2048 -validity 10000

//key store user and password
 koombea

// package name
 com.rhomobile.track_r

